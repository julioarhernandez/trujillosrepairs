backend:
  name: git-gateway
  branch: master

media_folder: "images/" # Folder where user uploaded files should go

collections: # A list of collections the CMS should be able to edit
  - label: "Pages"
    name: "home"
    files:
      - label: "Global"
        name: "Global"
        file: "_data/global/global.yml"
        fields:
          - {label: Brand, name: brand, widget: string, hint: "Brand Name"}
      - label: "Home Page"
        name: "home"
        file: "_data/home/home.yml"
        fields:
          - {label: Header, name: header.subtitle, widget: string, hint: "The Large Text at the top"}
          
